= Individual Contributions
// TODO: Briefly describe what each member contributed.
#figure(
  text(size: 8pt)[
    #table(
      columns: 2,
      align: (left, left),
      inset: 4pt,
      [*Member*], [*Primary area of ownership*],
      [Madeline Abio],
      [
        FCR and Search Radar controllers; Kalman `TrackFilter` and
        `BallisticTrajectoryPredictor`; `AtlasFSM` and the multi-condition convergence
        gate; `AttackPredictor` online learning component.
      ],

      [Alpa Sahai],
      [
        Webots world file, robot PROTOs, and scene setup; anti-friendly-fire safety
        exclusion zone; simulating the 3D radars; system architecture diagram;
        integration testing, report writing, and demo video.
      ],
    )
  ],
  caption: [Project contributions, with significant overlap via pair programming.],
)



